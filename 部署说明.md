# MES接口文档 - 部署说明

## 方案一：本地预览（最快）

```bash
# 进入项目根目录
cd F:\Obsidian\MES接口文档\Mes接口

# 启动 Python HTTP 服务
python -m http.server 8080

# 浏览器访问
# http://localhost:8080
```

## 方案二：GitHub Pages（免费公网）

### 步骤

1. **创建 GitHub 仓库**
   ```bash
   git init
   git add .
   git commit -m "MES接口文档"
   git remote add origin https://github.com/你的用户名/mes-api-docs.git
   git push -u origin main
   ```

2. **开启 GitHub Pages**
   - 进入仓库 Settings → Pages
   - Source 选择 `main` 分支，目录选 `/ (root)`
   - 点击 Save

3. **访问**
   - 地址: `https://你的用户名.github.io/mes-api-docs/`

## 方案三：Nginx 部署（公司内网）

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name mes-docs.yourcompany.com;

    # Docsify 需要的根目录
    root /var/www/mes-api-docs;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 支持中文文件名
    charset utf-8;
}
```

### 部署步骤

```bash
# 1. 复制文件到服务器
scp -r ./* user@server:/var/www/mes-api-docs/

# 2. 重启 Nginx
sudo systemctl reload nginx
```

## 方案四：Docker 部署

```bash
# 创建 Dockerfile
cat > Dockerfile << 'EOF'
FROM nginx:alpine
COPY . /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EOF

# 构建并运行
docker build -t mes-docs .
docker run -d -p 8080:80 mes-docs
```

## 文件结构说明

```
Mes接口/                    ← 项目根目录（也是 Docsify 根目录）
├── index.html              ← Docsify 入口文件
├── README.md               ← Docsify 首页内容
├── _sidebar.md             ← 左侧导航菜单
├── .nojekyll               ← GitHub Pages 必需
├── 部署说明.md              ← 本文档
├── 欢迎.md                  ← Obsidian 欢迎页
└── MES接口/                ← 接口文档目录
    ├── 00-总览.md
    ├── 用户信息配置界面接口/
    ├── MES交互接口/
    ├── HUD项目相关接口/
    ├── ...（共13个模块文件夹）
    └── 车灯3.0相关接口/
```

## 注意事项

- **Obsidian 特有语法**: `[[wikilink]]` 在 Docsify 中不会自动渲染为链接，但不影响阅读
- **Frontmatter**: YAML 头部信息会显示为源码文本，如需隐藏可配置 CSS
- **搜索功能**: Docsify 内置全文搜索，支持中文
